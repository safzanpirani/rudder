Build a small self-contained Python package in the current directory:

1. `primes.py` — generate the first 200 primes with a sieve.
2. `bench.py` — time the sieve against trial division.
3. `test_primes.py` — unittest coverage for both.
4. `README.md` — document all three.

Write and verify each file one at a time, in that order, and say what
you are doing before each step. Do not search the filesystem or read
skills; just build the package.
